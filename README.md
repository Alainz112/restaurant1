# Restaurant/Cafe Landing Template (V4 — Dark/Light toggle)

- Dark mode + light mode toggle (saved in localStorage)
- Default theme in `data/site.json` → `defaultTheme` ("dark" or "light")
- Menu images per item + modal image
- Scroll reveal animations

Deploy:
- Vercel: Framework Other • Build Command None • Output Directory .
- Netlify: Build command empty • Publish directory .

Local preview: use VSCode “Live Server”.
